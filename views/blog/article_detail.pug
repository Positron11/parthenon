extends ../base

append stylesheets 
	link(rel="stylesheet", href="/stylesheets/blog/article_detail.css")

prepend header
	- var heading = article.title
	- var subheading = article.subtitle
	- var content = article.renderDescription()

block content	
	.separator 

	#article_content!= article.renderContent()

block navbar_buttons 
	a.unstyled(href=`${article.url}/edit`): img(src="/images/icons/edit.svg").navbar-button-icon
	a.unstyled(href=`${article.url}/delete`): img(src="/images/icons/trash.svg").navbar-button-icon